name: Deploy
on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    env:
      REGISTRY: ghcr.io/${{ github.repository }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push backend image
        run: |
          docker compose -f backend/compose.build.yml build
          docker compose -f backend/compose.build.yml push
      - name: Build and push frontend image
        run: |
          docker compose -f frontend/compose.build.yml build
          docker compose -f frontend/compose.build.yml push

  deploy:
    name: Deploy
    needs: [build]
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    env:
      REGISTRY: ghcr.io/${{ github.repository }}

      DB_DATABASE: ${{ secrets.DB_DATABASE }}
      DB_USERNAME: ${{ secrets.DB_USERNAME }}
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      DB_PORT: ${{ secrets.DB_PORT }}

      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      JWT_EXPIRES_IN: ${{ secrets.JWT_EXPIRES_IN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      # - name: Clean containers, image
      #   run: docker compose -f backend/compose.yml -p smart-lock-backend down --rmi all -v
      - name: Remove old iamge
        run: docker image prune -a -f
      - name: Deploy Backend
        run: docker compose -f backend/compose.yml -p smart-lock-backend up -d
      - name: Deploy Frontend
        run: docker compose -f frontend/compose.yml -p smart-lock-frontend up -d
